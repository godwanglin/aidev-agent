# Aidev CLI (`aidev`) Reference

The Aidev CLI (`aidev`) is a lightweight, terminal-based interface for fast
agent interaction. For full details, always consult the live public
documentation:

-   **CLI Features & Subagents**: `https://aidev.google/docs/cli/features`
-   **CLI Best Practices**: `https://aidev.google/docs/cli/best-practices`
-   **CLI Reference**: `https://aidev.google/docs/cli/reference`

When the user asks about CLI specifics, **fetch the relevant page above** for
authoritative, up-to-date information.

--------------------------------------------------------------------------------

## 1. Getting Started

-   **Launch**: Run `aidev` to start the CLI.
-   **Authentication**: On first run, follow the on-screen prompts to
    authenticate. See `https://aidev.google/docs/cli/reference` for
    details.
-   **Exit**: `Ctrl+D Ctrl+D` (or `/exit` or `/quit`).

--------------------------------------------------------------------------------

## 2. CLI Slash Commands

-   **CLI flags & subcommands**: Run `aidev --help` to see all command-line flags
    and subcommands.
-   **Slash commands** (inside the TUI): Launch `aidev` and run `/help` to see all
    available slash commands.
-   **Full reference**: Fetch `https://aidev.google/docs/cli/reference`
    for the authoritative list of all slash commands and CLI options.

--------------------------------------------------------------------------------

## 3. Configuration

The CLI is configured via **`~/.aidev/aidev-cli/settings.json`**. For the
full list of settings keys, types, and defaults, fetch the live docs:
`https://aidev.google/docs/cli/reference`
